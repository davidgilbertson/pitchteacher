{
  "name": "pitchteacher",
  "private": true,
  "version": "1",
  "type": "module",
  "scripts": {
    "dev": "vite --port 8082",
    "build": "vite build",
    "preview": "npm run build && vite preview --port 8082 --strictPort",
    "deploy": "npm run build && firebase deploy --only hosting"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "vite": "^5.4.0"
  }
}
